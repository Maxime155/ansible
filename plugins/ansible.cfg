[defaults]
# les temps des tasks
callbacks_enabled=ansible.posix.profile_tasks
# les sorties erreur en YAML
stdout_callback = yaml
bin_ansible_callbacks = True
# n'affichage pas les ok
display_ok_hosts = no
